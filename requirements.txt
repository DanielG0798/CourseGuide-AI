# Core
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
httpx>=0.27

# Model client abstraction
openai>=1.40
openai-agents>=0.1.0

# MCP (pin to 1.x to match the FastMCP API used in the course examples)
mcp>=1.0,<2.0

# Evaluation and testing
pytest>=8.0
pytest-asyncio>=0.24

# Knowledge / memory layer (M3)
chromadb>=0.5
sentence-transformers>=3.0

# Utilities
jinja2>=3.1
rich>=13.0
